class myPiece:
    """
    Custom built piece object for storing piece information
    """
    def __init__(self, file, rank, type, color):
        self.file = file
        self.rank = rank
        self.type = type
        self.color = color

